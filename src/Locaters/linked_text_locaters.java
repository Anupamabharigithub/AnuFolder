package Locaters;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

public class linked_text_locaters {
	public static void main(String[] args) {
		/*
		 * String givenURL="https://demowebshop.tricentis.com/"; String
		 * logintitle="Demo Web Shop. Login"; WebDriver driver=new FirefoxDriver();
		 * //enter the DEMo web page driver.get("https://demowebshop.tricentis.com/");
		 */
		//maximize the brwser
		//driver.manage().window().maximize();
		//URL verification
		/*
		 * if(givenURL.equals(currentURL)) {
		 * System.out.println("You are in login page");
		 * driver.findElement(By.linkText("Log in")).click(); String
		 * currentlogintitle=driver.getCurrentUrl(); driver.close(); }
		 */ /*
			 * else System.out.println("You are in not login page");
			 */
	
		//String currentURL=driver.getCurrentUrl();
	}
	
	//verify the current URL

}
